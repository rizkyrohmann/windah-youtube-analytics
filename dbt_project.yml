name: 'windah_youtube_analytics'
version: '1.0.0'
config-version: 2

profile: 'windah_youtube_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  windah_youtube_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table